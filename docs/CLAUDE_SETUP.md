# Claude Code setup

Use the direct CLI fallback from a repository root:

```powershell
zoracleflux check --json
```

`adapters\claude\SKILL.md` is a copyable skill instruction. No Claude client is
assumed or invoked by the release candidate; validate the fallback command.
