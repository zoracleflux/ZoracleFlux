# Codex setup

Use `zoracleflux check --json` as a read-only CI or agent step. The adapter
manifest documents the tested fallback and intentionally does not infer an
OpenAI endpoint, API key, or model. No Codex client was available in the local
release rehearsal.
