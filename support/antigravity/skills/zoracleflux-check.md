---
name: zoracleflux-check
description: Run bounded offline ZoracleFlux checks and report JSON evidence.
---

Use the configured `zoracleflux` MCP server and call `zoracleflux_check`.
Report the returned status, counts, version, network flag, and model-call count.
Do not claim native Antigravity compatibility unless the Antigravity client
itself has been installed and the connection is observed in its MCP manager.
