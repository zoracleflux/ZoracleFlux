# Security policy template — requires owner and independent review

**Draft status:** This document does not establish a security guarantee.

Document supported versions, threat model, reporting channel and PGP/key
handling, severity levels, response targets (targets are not an SLA), disclosure
policy, patch cadence, dependency scanning, access reviews, secret rotation,
backup encryption, logging/retention, isolation, incident response, and
security-contact ownership. Reference the release candidate's `docs\SECURITY.md`
for current boundaries. Replace every placeholder before publishing.

