# Integrated workstream reports

These are copied, read-only provenance artifacts for the RC3 integrator. The
executable integration lives at the RC3 root; the original workstream folders
remain outside this bundle and are preserved.

* `claude-codex`: fallback/plugin/skill/MCP report; native clients unverified.
* `signing`: local OpenSSH Ed25519 signature report; 4/4 detached checks pass,
  but trust is not organization-authenticated.
* `production`: production-shaped local pilot evidence and Gate B templates.
* `..\antigravity`: first-class platform registration/MCP report; native
  authenticated call unverified because isolated account state was unavailable.
